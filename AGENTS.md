# 项目开发规则

本文件适用于本仓库内今后的所有开发、检查和文档工作。开始任务前，先阅读本文件、`PROJECT_CONTEXT.md`、`PROJECT_STATUS.md`、`ROADMAP.md` 和 `CONTENT_AND_ASSET_RULES.md`。

## 1. 项目边界

- 这是已经存在的“河南音乐考古资源数字活化”网站，不得另建项目替代现有项目。
- 不得重写整个网站，不得擅自迁移技术栈，不得以“整理”为由大规模改目录。
- 继续使用 React、TypeScript、Vinext、Vite、Three.js、自定义 CSS 和 TypeScript 数据文件。
- 当前不引入正式数据库。确需启用数据库、登录、云存储或大模型接口时，必须单独立项并说明必要性。
- 每次只完成一个范围明确、能够单独测试和验收的小任务。

## 2. 专业资料边界

- 技术人员和 AI 不负责创造、推断或判断考古学结论。
- 不得自行补充或修改文物年代、用途、材质、尺寸、音色、出土信息、历史场景、研究价值和争议结论。
- 页面专业内容只能来自团队提供并经过审核的资料；修改前须确认来源、审核状态和更新时间。
- 资料不足时使用明确标注的占位数据或演示素材，不得用看似真实的内容填空。
- 程序生成模型必须标注为数字功能演示模型；数字合成声音必须标注为合成演示音效；AI 生成图片或视频必须标注为 AIGC 内容。
- 不得把程序生成模型、AI 生成内容或数字合成声音表述为真实扫描、考古复原、原器录音或确定的历史场景。
- 不得由开发人员把“待专业审核”改成“已审核”。审核结论、审核人和审核时间必须由团队提供。

## 3. 修改流程

1. 先检查 Git 状态、相关源文件、现有数据结构和测试，不覆盖用户已有修改。
2. 明确本次唯一目标、输入资料、涉及文件和验收标准。
3. 优先做最小修改，避免顺手重构无关代码、升级整套依赖或删除模板文件。
4. 新增文物时复用统一数据结构和详情页面，不复制整套页面。
5. 页面必须继续支持 375px 手机宽度和 1440px 电脑宽度。
6. 3D、音频、AI、图片和网络功能必须保留异常提示或静态降级，单项失败不得拖垮整页。
7. 修改后至少运行与改动相关的类型检查、构建和测试；涉及交互或布局时补充对应的浏览器验收。
8. 不在前端代码、Git 历史或文档中写入 API 密钥、访问令牌和隐私数据。

## 4. 依赖与命令

- 仓库已有 `package-lock.json` 和 `node_modules`，继续使用 npm，不擅自切换 pnpm、yarn 或 bun。
- 安装依赖前先检查现有依赖和锁文件；没有明确需要时不重新安装。
- Windows PowerShell 可能拦截 `npm.ps1`，可使用 `npm.cmd`，不要为了运行项目修改用户的系统执行策略。
- 当前常用命令：
  - 开发：`npm.cmd run dev`
  - 构建：`npm.cmd run build`
  - 启动构建产物：`npm.cmd run start`
  - 现有测试：`npm.cmd test`
  - Lint：`npm.cmd run lint`
- 当前完整类型检查仍有 Cloudflare Worker 类型问题，处理前参见 `PROJECT_STATUS.md`，不得用跳过检查掩盖错误。

## 5. 完成报告

每次任务结束必须报告：

- 修改了哪些文件；
- 是否修改专业资料；
- 运行了哪些命令及结果；
- 哪些内容已经验收，哪些只做了静态检查；
- 仍存在的问题和风险；
- 下一项建议任务；
- 是否依赖团队补充资料或审核。

