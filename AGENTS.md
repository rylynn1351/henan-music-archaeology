# Codex 开发规则

本仓库后续所有任务必须遵守以下规则：

- 不迁移现有技术栈，不重写整个项目。
- 不擅自修改、补充或推断考古事实、年代、用途、音色、历史场景和研究结论。
- 缺少专业资料时，只使用明确标注的占位内容或演示素材。
- 每次只完成一个能够独立测试和验收的小任务。
- 修改前先检查 Git 状态、现有实现、数据结构和相关测试。
- 优先复用现有组件和数据结构，不复制页面，不进行无关重构。
- 不随意增加、删除或升级依赖；继续使用 npm 和现有 `package-lock.json`。
- 修改后必须运行构建和与本次修改相关的测试；失败时如实记录，不得通过关闭检查掩盖错误。
- 涉及页面时必须检查 375px 手机端和 1440px 电脑端。
- 3D、音频、AI、图片和网络等单项功能失败时，必须保留提示或静态降级，不得导致整页白屏。
- 程序生成模型必须标注为数字演示模型；数字合成音频必须标注为合成演示音效；AIGC 图片和视频必须明确标注。
- 不得把演示模型、合成声音或 AIGC 内容表述为真实扫描、真实音色、原器录音或正式考古复原。
- 不在前端、Git 历史或文档中写入 API 密钥、令牌、密码和个人隐私信息。
- 完成后必须汇报：修改文件、测试命令、测试结果、剩余问题、团队资料依赖和下一步建议。

