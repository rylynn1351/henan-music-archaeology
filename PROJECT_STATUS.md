# 项目当前状态

## 状态快照

- 检查日期：2026-08-17
- 当前阶段：v0.5 多文物展示与交互版本收口
- 当前分支：`codex/handoff-baseline`
- 远程仓库：`https://github.com/yyyyyyuts/henan-music-archaeology.git`
- Node.js：本地 v24.15.0；CI 使用 Node.js 22
- 内容记录：3 条；贾湖骨笛为明确标注的数字演示，文物 002、003 为公开“资料整理中”占位记录
- 正式新增文物：0 条；本轮未录入或推断新的考古事实
- 质量状态：`npm run verify` 通过，文物资料预检 0 错误、0 警告，自动测试 46/46 通过

## 当前已完成能力

### 页面与路由

- `/` 为项目门户和重点文物入口，不再重复承载完整详情。
- `/artifacts` 为独立文物总览，支持名称搜索、年代/时期、材质和器物类型组合筛选。
- `/artifacts/[slug]` 为通用详情路由；正式演示记录进入完整体验，占位记录进入统一的“资料整理中”页面。
- 未知、非法或不可展示 slug 使用友好 404；占位详情带 `noindex, nofollow`。
- 002、003 共用宽版占位详情模板，桌面端使用标准 1180px 内容宽度，手机端保持安全边距。

### 文物数据与接入

- 一件文物一个 TypeScript 数据文件，由统一注册表提供内部、目录可见和正式详情查询。
- `artifact:new` 生成并注册 `draft + internal` 草稿，保护重复编号、slug、非法 slug 和已有目标文件。
- `artifact:check` 检查注册、字段引用、来源定位、审核日期、授权状态和图片/GLB/音频文件，并按文物输出中文报告。
- 正式记录区分内容审核和素材审核；正式公开素材只接受 `authorized` 或 `open_license`。
- 图片、模型、音频、来源、时间线、问答和演示性质均使用结构化可选字段，缺失时局部降级。

### 3D、音频与联动

- `ArtifactModelViewer` 支持程序化 Demo 和经授权 GLB、旋转、缩放、自动旋转、复位、延迟加载及备用图片降级。
- 通用模型热点框架支持投影定位、说明面板和关联音频联动；当前贾湖演示页为避免遮挡笛孔，未启用可见热点编号。
- `ArtifactAudioPlayer` 支持浏览器合成和文件音频、音轨切换、播放、进度、音量及失败降级。
- `ArtifactWaveform` 会解码音频并缓存包络，以居中细柱绘制波形；支持已播放着色、Canvas 点击/拖动定位和可访问进度条。
- 合成声音继续明确标注为数字合成演示音效，不表述为原器、原件或复原乐器录音。

### 智能讲解与数字纪念

- 智能讲解继续使用本地审核问答和固定安全答复，不联网、不上传问题、不伪装成正式 AI/RAG。
- `ArtifactCommemorativeCard` 可在浏览器本地生成品牌纪念卡预览和 PNG 下载。
- 可选昵称只参与本次图片生成，不上传、不持久化；纪念卡不写入未经审核的考古结论。
- 纪念卡具备弹窗焦点循环、关闭恢复焦点、图片/Canvas/下载失败提示和模块错误降级。

### 页面展示收口

- 贾湖详情页时间线编号已提高对比度，桌面、平板和手机布局保持一致。
- 声音体验波形不再形成整块填充，底部进度控件保留独立空间。
- 文物详情只保留可核验的资料来源，不再展示产品参考和底层开源技术说明栏。
- 3D、音频、图片、问答和纪念卡任一模块失败时不会导致整页白屏。

## 2026-08-17 验收结果

| 检查 | 结果 |
| --- | --- |
| `npm run verify` | 通过 |
| ESLint / TypeScript | 通过 |
| 文物资料预检 | 0 错误、0 警告 |
| 生产构建 | 通过；首页、总览和动态详情路由生成成功 |
| 自动测试 | 46/46 通过 |
| 1440×900 | 贾湖详情、002 占位详情、波形、时间线和来源区无横向溢出 |
| 375×812 | 占位详情和声音体验保持单列，播放器及文字完整 |
| 波形交互 | 原生进度条和 Canvas 点击定位均已实际验证 |
| 资料边界 | 未增加考古事实，演示模型和合成音频标识保留 |

## 发布与接入流程

1. 团队在仓库外层私有 `项目资料/文物资料交付模板/` 提交文字、来源、素材和授权文件，空缺内容写“待确认”。
2. 使用 `npm run artifact:new -- <id> <slug> <displayIndex>` 生成内部草稿，保持主记录和子记录 ID 稳定。
3. 录入内容、来源定位和素材元数据；内容审核与素材审核分别完成。
4. 只把审核与授权完成的文件复制到 `public/artifacts/<slug>/`。
5. 运行 `npm run verify`；人工确认两类审核记录后，才把状态改为 `approved` 或 `published`。
6. 通过 Git 分支、代码审查和部署平台完成发布；项目没有自动把草稿转为正式记录的命令。

## 当前阻塞与已知问题

- 团队尚未交付文物 002、003 的正式名称、专业资料、来源定位和授权素材，因此继续保持中性占位状态。
- `.openai/hosting.json` 保留原 Sites 项目 ID；该项目的可访问性和正式部署安排仍需团队确认。
- Vinext 仍提示部分动态路由无法在构建时完全分类；当前生产构建、服务端渲染和本地页面均正常。
- 微信 WebView、馆内设备和正式部署域名仍需真实设备验收。

## 团队仍需提供

- 正式文物清单、稳定编号和名称确认。
- 专业文字、研究争议、时间线、问答及逐项来源定位。
- 内容审核人、审核日期和资料版本。
- 图片、GLB、音频的制作者、来源、授权范围、署名文本，以及素材审核人和审核日期。
- GLB 的单位、比例、朝向、备用图和模型性质说明。
- 部署平台、域名、备案和目标设备验收安排。

## 下一步

当前展示框架已具备独立总览、通用详情、占位详情、3D/热点框架、波形播放器、本地问答和数字纪念卡，暂无必须继续扩展的页面功能。收到首批正式资料后，应完整执行一次“生成草稿 → 录入 → 两类审核 → 素材复制 → `verify` → 代码审查 → 发布”，再根据真实资料暴露的问题调整模板和校验规则。
