# 项目当前状态

## 1. 状态快照

- **当前版本：** 贾湖骨笛数字展示 Demo v0.2
- **检查日期：** 2026-07-23
- **当前分支：** `main`
- **页面入口：** `/`
- **包管理器：** npm（存在 `package-lock.json`）
- **运行环境：** Node.js v24.15.0、npm 11.12.1，满足项目声明的 Node.js `>=22.13.0`
- **内容状态：** 页面数据仍标记为“待专业成员审核”；不得用于正式研究结论发布

## 2. 当前可以运行的功能

- 贾湖骨笛单件文物档案；
- 参考图片、基本资料、时间线、来源和更新时间；
- 程序生成的 Three.js 3D 演示模型及旋转、缩放、复位；
- 浏览器生成的数字合成演示声音；
- 本地规则问答；
- Demo、模型和声音性质说明；
- 720px、980px 响应式断点和减少动画样式。

以上“可以运行”来自源码检查、构建、服务端渲染测试和本地 HTTP 冒烟测试。3D 手势、音频实际播放、问答点击、375px/1440px 最终视觉和微信内置浏览器尚无自动化浏览器测试证据。

## 3. 本轮命令与结果

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 依赖状态 | `npm.cmd ls --depth=0` | 通过；声明依赖可解析，另有 5 个 `@emnapi/@tybys` 顶层 extraneous 项，暂不重装 |
| 正式构建 | `npm.cmd run build` | 通过；Vinext 完成 5 个构建阶段 |
| 现有测试 | `npm.cmd test` | 通过；2 项通过，0 项失败 |
| 构建产物启动 | `npm.cmd run start -- --host 127.0.0.1 --port 4173` | 启动后根页面 HTTP 200，返回内容包含 Demo 标题 |
| 完整类型检查 | `node_modules/.bin/tsc.cmd --noEmit` | 失败；3 个 Cloudflare Worker 类型错误 |
| 项目 Lint 脚本 | `npm.cmd run lint` | 失败；扫描了 `work/` 临时产物，且项目源码有 1 个错误、1 个警告 |
| 核心源码 Lint | 对 `app/db/worker/tests` 和根配置执行 ESLint | 失败；`HeritageDemo.tsx` 1 个错误，`ArtifactDetail.tsx` 1 个警告 |

类型错误：

- `db/index.ts` 找不到 `cloudflare:workers`；
- `worker/index.ts` 找不到 `Fetcher`；
- `worker/index.ts` 找不到 `D1Database`。

Lint 核心问题：

- `HeritageDemo.tsx` 在 effect 中同步设置演示音频 URL，触发 `react-hooks/set-state-in-effect`；
- `ArtifactDetail.tsx` 使用普通 `<img>`，触发性能警告；
- 全仓库 Lint 没有排除 `work/` 等检查产物，造成大量与产品源码无关的输出。

构建提醒：

- Vinext 当前不能完全静态分类根路由；
- 客户端存在大于 500 kB 的构建块，Three.js 首屏加载需要后续在手机和微信环境实测。

## 4. 已知问题（按当前优先级）

### 高

1. **类型检查基线未通过。** 构建虽然成功，但完整 TypeScript 检查缺少 Cloudflare Worker 类型，后续改动缺少可靠的静态安全网。
2. **专业内容仍待团队审核。** 页面已经明确显示待审核，但正式公开或比赛交付前必须逐条确认来源、审核人和审核时间，开发人员不能自行改成已审核。
3. **国内可访问部署尚未解决。** 现有 `chatgpt.site` 地址有已知访问受阻记录；仓库没有正式国内域名、备案或多网络验收方案。
4. **3D 区域没有异常降级。** WebGL、观察器或显卡初始化失败时可能空白或报错，低端手机和微信 WebView 的演示风险较高。

### 中

5. **数据和 UI 仍有耦合。** 首屏数字、部分专业文字、3D 几何、音高和问答提示直接写在 `HeritageDemo.tsx`，绕过统一审核数据。
6. **页面组件过重。** 一个约 443 行的客户端组件同时负责布局、3D、WAV 生成和问答，后续多文物扩展容易重复。
7. **资源数据结构不完整。** 模型类型允许 `glb`，但没有模型 URL、热点和授权字段；音频也没有文件 URL、来源和授权字段。
8. **来源绑定存在扩展错误。** 文物详情使用本件文物来源，但页面底部来源区遍历全局来源；加入第二件文物后可能混入无关来源。
9. **Lint 基线不可直接使用。** 脚本范围包含临时工作目录，核心代码本身还有 1 个规则错误。
10. **测试覆盖有限。** 现有两项测试不验证 3D、音频、问答、外链、失败降级、375px/1440px 和微信环境。
11. **移动端只有样式证据。** 已有断点和触控样式，但尚未形成 375px、1440px 和微信浏览器的可重复验收记录。
12. **首屏资源偏大。** Three.js 和整个客户端页面一起加载，国内移动网络下需要测速并优先提供静态降级。

### 低

13. README、认证辅助文件、空数据库和 D1 示例仍保留 starter 内容，容易让初学者误判项目核心。
14. 3D 清理没有显式释放所有端部和音孔几何体，开发热更新或反复挂载时可能积累 GPU 资源。
15. 当前图片未显式设置尺寸，可能产生布局偏移；授权信息已有基础标注，但缺少统一资产台账和授权凭证位置。
16. `tmp/` 当前未被 Git 跟踪，也未被 `.gitignore` 排除；它是检查/资料处理目录，不应误提交为产品源码。

## 5. 当前缺少的资料

- 每条现有专业陈述的最终审核结果、审核人和审核时间；
- 正式文物清单和第二件文物的完整审核资料；
- 图片、模型、音频、视频的原文件和授权凭证；
- 真实 GLB 的比例、朝向和热点资料；
- 声音类别及制作/录制说明；
- AI 讲解使用的审核资料、标准问答和引用规则；
- 正式项目视觉资料、版权主体和隐私文本；
- 国内可访问域名、部署平台、备案和小程序主体资料。

## 6. 本轮修改

本轮仅新增或完善以下项目文档：

- `AGENTS.md`；
- `PROJECT_CONTEXT.md`；
- `ROADMAP.md`；
- `PROJECT_STATUS.md`；
- `CONTENT_AND_ASSET_RULES.md`。

没有修改页面源码、专业数据、依赖、锁文件或部署配置，也没有发布网站。

## 7. 下一项建议任务

只做“修复 TypeScript 类型检查基线”这一项：为现有 Cloudflare Worker 代码配置正确类型，并在 `package.json` 增加 `typecheck` 命令。完成后要求类型检查、构建和现有测试全部通过，不改页面外观和考古资料。

这个任务不依赖团队资料，改动范围小，而且能给以后每次修改提供明确反馈。完成它之后，再单独做“3D 初始化失败时显示静态参考图和提示”。

