# 项目当前状态

## 1. 状态快照

- **当前版本：** v0.3 多文物展示基础版（当前正式数据仍只有贾湖骨笛）
- **检查日期：** 2026-07-23
- **正式交接目录：** `D:\learing\competence\大创赛--音乐考古\henan-music-archaeology`
- **当前分支：** `codex/handoff-baseline`
- **远程仓库：** 无
- **页面入口：** `/`、`/artifacts/[slug]`
- **包管理器：** npm
- **Node.js：** v24.15.0，符合项目声明的 `>=22.13.0`
- **内容审核状态：** 现有页面仍显示“待专业成员审核”
- **工作区状态：** Lint 基线、v0.3 文档和五个正式开发任务的修改均尚未提交 Git

## 2. 当前可以运行的功能

- 贾湖骨笛单件文物档案；
- 参考图片、基本资料、时间线、来源和更新时间；
- 程序生成的 Three.js 3D 演示模型；
- 3D 旋转、缩放、自动旋转和复位；
- 浏览器生成的数字合成演示音频；
- 本地规则问答和推荐问题；
- Demo、模型和声音性质说明；
- 由统一数据驱动的文物总览区域和通用文物卡片；
- 文物名称搜索、年代或时期、材质和器物类型筛选；
- 当前结果数量、重置筛选和无匹配结果恢复；
- 基于统一 `slug` 的 `/artifacts/[slug]` 通用详情路由；
- 文物卡片进入独立详情页，详情页可返回根页面文物总览；
- 不存在、格式异常、占位或不可展示文物的友好 404 处理；
- 详情路由运行时异常的友好错误边界；
- 首页只负责项目门户、文物总览、搜索筛选、卡片和Demo说明，不再固定渲染完整详情；
- `ArtifactExperience` 作为单件文物完整体验容器，只由动态详情路由使用；
- 主图片缺失或加载失败时显示固定比例占位，不影响档案文字；
- 3D初始化或渲染失败时显示备用图片和友好提示，不影响音频、问答和档案；
- 音频创建、加载或播放失败时显示提示并释放Object URL；
- 时间线、来源或本地问答为空时显示明确的待补充状态；
- 3D、音频和问答具有独立模块错误边界，单项意外错误不会替换整个详情页；
- 响应式 CSS 和减少动画样式。

以上来自源码检查、成功构建、服务端渲染测试和本地 HTTP 冒烟测试。

## 3. 当前无法确认

- 现有考古内容是否已经完成逐条专业审核；
- 当前图片来源与实际 JPG 文件是否已经完成最终版权核对；
- 3D 在低端手机、关闭硬件加速和部分微信 WebView 中是否稳定；
- 音频能否在目标浏览器中正常播放、暂停和再次播放，仍需人工点击检查；
- 375px 和 1440px 的最终视觉效果是否没有溢出、遮挡、变形或异常裁切，仍需人工检查；
- 微信内置浏览器的触控、音频和 3D 兼容性；
- Windows 浏览器自动化因无法可靠确认 Edge 当前页面 URL，被安全机制中止；本轮没有绕过该机制，也没有把浏览器检查记录为通过；
- 375px、1440px 下从卡片进入详情、刷新详情和返回总览的真实浏览器流程仍需人工检查；
- 图片失败、WebGL禁用和音频媒体错误的真实浏览器降级效果仍需人工触发检查；
- 现有公开演示地址在国内不同运营商网络中的稳定性；
- 计划中的多文物、真实 GLB、模型热点、AI 讲解、纪念卡、AIGC 视频和小程序功能尚未实现。

## 4. 实际运行结果

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 安装依赖 | `npm.cmd ci` | 成功，按锁文件安装 514 个包 |
| Cloudflare 类型依赖 | `npm.cmd install --save-dev --save-exact @cloudflare/workers-types@4.20260515.1` | 成功，只增加 1 个官方开发依赖 |
| 类型检查 | `npm.cmd run typecheck` | 成功，0 个错误 |
| Lint | `npm.cmd run lint` | 成功，0 个错误、0 个警告 |
| 构建 | `npm.cmd run build` | 成功 |
| 现有、数据、路由与降级测试 | `npm.cmd test` | 成功，12 项通过、0 项失败 |
| 本地 HTTP 检查 | `vinext dev --host 127.0.0.1 --port 4175` + `Invoke-WebRequest` | 成功：首页和贾湖详情 HTTP 200；不存在的文物详情 HTTP 404，并包含友好提示和返回总览入口；开发预览CSS HTTP 200 |

原类型检查错误：

- `db/index.ts(1,21)`：找不到 `cloudflare:workers` 模块或类型声明；
- `worker/index.ts(6,11)`：找不到 `Fetcher`；
- `worker/index.ts(7,7)`：找不到 `D1Database`。

根本原因与修改方式：

- 现有 Wrangler 仅把 `@cloudflare/workers-types` 列为可选 peer dependency，项目实际没有安装或加载 Cloudflare Worker 官方类型；
- 增加与当前 Wrangler 兼容的精确版本 `@cloudflare/workers-types@4.20260515.1`；
- 在 `tsconfig.json` 中显式加载 Node 和 Cloudflare Worker 类型；
- 新增 `worker/env.d.ts`，只声明项目现有的可选 D1 `DB` 绑定；
- 在 `package.json` 中增加 `typecheck` 脚本：`tsc --noEmit --incremental false`；
- 未使用 `any`、`@ts-ignore`、`@ts-nocheck`，未关闭 `strict`，未排除 Worker 目录，也未修改 Worker 运行时代码。

原 Lint 问题与修改方式：

- `HeritageDemo.tsx` 原先在 effect 内同步设置音频 URL state，触发 `react-hooks/set-state-in-effect`；
- 改为用 `audio` 元素 ref 同步 Object URL：每次组件挂载创建一次，普通渲染不会重复创建；卸载时暂停音频、移除 `src`、调用 `load()`，最后撤销同一个 Object URL；
- `ArtifactDetail.tsx` 原先使用普通 `<img>`，触发 `@next/next/no-img-element` 性能警告；
- 改用当前 Vinext 已支持的 `next/image`，继续使用原图片地址、替代文本和现有 CSS；按实际图片设置 `1920×1440`，使用懒加载，并通过 `unoptimized` 避免改变本地图片请求路径；
- 未增加依赖，未添加 ESLint 禁用注释，未使用 `any`、`@ts-ignore` 或 `@ts-nocheck`。

其他安装和构建提示：

- `npm.cmd ci` 报告 17 项依赖安全告警，其中 11 项为 high；本轮没有自动执行可能升级依赖的 `npm audit fix`；
- 构建提示客户端存在大于 500 kB 的代码块；
- Vinext 暂时不能完全静态判断根路由类型。
- 当前配置下直接使用 `vinext start` 时曾出现HTML返回200但构建资产 `/assets/*` 返回404；`vinext dev`预览正常。正式部署前必须单独验证生产静态资源服务，不能只检查HTML状态码。

## 5. 已知问题

### 严重

当前没有发现已经确认的密钥泄露、无法构建或数据破坏问题。

### 高

1. 现有专业内容仍待团队审核，正式公开前不能自行改成已审核。
2. 国内稳定部署方案、正式域名和网络验收尚未确定。
3. 本地 `vinext start` 的构建资产服务存在待定位的404风险，正式部署前需验证。
4. 依赖安装报告多项 high 安全告警，需要单独分析直接影响，不能直接使用强制升级处理。

### 中

5. `ArtifactExperience.tsx` 仍同时包含3D、音频和问答内部实现；这是本轮刻意保留的最小迁移边界。
6. 部分展示文字、3D 几何和合成音高仍有硬编码。
7. 当前只录入一件文物，正式第二件文物仍依赖团队审核资料。
8. 尚未实现集中数据校验；动态详情路由已经建立。
9. 自动测试验证了降级分支和页面输出，但没有模拟真实WebGL/媒体失败，也未覆盖3D、音频、问答实际点击、375px、1440px 和微信环境。
10. Three.js 跟随整个客户端页面加载，首屏资源偏大。

### 低

11. README、空数据库和 D1 示例仍有 starter 模板痕迹。
12. 当前只完成程序化3D的异常降级，尚未形成通用GLB加载器。

## 6. 缺少资料

- 现有专业陈述的最终审核人、审核结论和审核时间；
- 正式文物清单和第二件文物完整资料；
- 图片、模型、音频和视频授权凭证；
- 真实 GLB 的比例、朝向和热点资料；
- 专业声音文件及其准确分类；
- AI 讲解的审核资料、标准问答和引用规则；
- 正式视觉规范、版权主体和隐私文本；
- 国内域名、部署平台、备案和小程序主体资料。

## 7. 最近修改内容

- 修复音频 Object URL effect 内同步设置 state 的 Lint 错误，保留 WAV 生成、原生音频控件和卸载清理；
- 将文物图片改为 Vinext 兼容的 `next/image`，继续使用原图片、原替代文本和原有样式；
- 建立统一的 `Artifact` 数据结构，补充内容、审核、声音、模型和数字资产分类；
- 将现有贾湖骨笛资料迁移到统一结构，现有页面改为通过安全查询函数读取可选字段；
- 增加 `getAllArtifacts()`、`getArtifactBySlug()`、主图、主音频、来源和档案字段查询函数；
- 明确当前模型为 `programmatic_demo`，当前声音为 `digitally_synthesized`；
- 当前数据集中仍只有贾湖骨笛一件文物，没有新增未经审核的第二件正式文物；
- 增加只返回明确允许展示记录的 `getDisplayableArtifacts()`，内部、占位和未设置可见性的记录默认不进入总览；
- 建立文物总览区域和可复用 `ArtifactCard`，当前正式页面只显示一张贾湖骨笛卡片；
- 卡片“查看详情”改为使用统一数据中的 `artifact.slug` 进入 `/artifacts/[slug]`；
- 增加名称搜索和年代或时期、材质、器物类型筛选，多个条件采用同时满足关系；
- 筛选选项由允许展示的文物自动生成，自动去重并排除空值；
- 增加结果数量、禁用态重置和无匹配结果恢复入口；
- 新增 `getDisplayableArtifactBySlug()`，直接访问详情地址时仍执行占位和展示权限过滤；
- 将首页和详情职责分离：`HeritageDemo` 只渲染项目门户、总览、搜索筛选和卡片，不再输出完整贾湖详情；
- 新增 `ArtifactExperience` 单件完整体验容器，动态详情路由通过它复用档案、时间线、3D、音频、问答和来源；
- 展示权限统一为：普通公开记录必须为 `approved` 或 `published`；`placeholder`、`under_review`、普通公开 `draft` 和内部记录不可访问；现有贾湖记录以明确的 `demo + isDemo + draft` 作为概念验证展示例外；
- 增加路由级 `not-found.tsx` 和 `error.tsx`，未知、异常格式、占位、不可展示或读取失败时不显示内部堆栈；
- 当前正式数据仍只有贾湖骨笛，没有新增第二件正式文物；
- Lint、类型检查、构建和 12 项测试均通过；
- 本地 HTTP 检查确认首页和贾湖详情返回 200，不存在的详情返回 404；
- 增加统一图片加载失败占位，详情和卡片都保留原替代文本语义；
- 3D初始化和动画渲染增加模块内错误捕获、备用图片降级及完整几何/材质清理；
- 音频生成、Object URL创建和媒体加载增加错误提示，失败或卸载时释放资源；
- 空时间线、空来源和空问答增加明确待补充状态；
- 3D、音频和问答分别使用模块错误边界，单项失败不影响其他详情内容；
- 本地问答匹配提取为可测试纯函数，无匹配或异常数据继续使用固定安全答复；
- v0.3页面版本标识完成收口，正式数据仍只有贾湖骨笛；
- Windows 浏览器自动化被安全机制中止，375px、1440px 和音频实际点击仍待人工验收；
- 没有新增依赖，没有修改文物专业内容；3D修改仅限错误捕获、资源清理和失败降级；
- 本次修改尚未提交 Git。

## 8. 下一项推荐开发任务

下一项建议不再开发新功能，只进行人工验收和Git基线整理：检查375px、1440px、卡片进入详情、刷新、返回、3D、音频、问答及三类真实失败路径；随后按既有任务拆分或确认累计提交范围。正式公开前还需团队完成专业审核和资产授权。

## 9. v0.3 文档基线

- 已创建 `SOFTWARE_REQUIREMENTS_SPECIFICATION.md` 和 `ARCHITECTURE.md`；
- 下一版本准备进入“v0.3 多文物展示基础版”；
- v0.3 范围固定为项目首页、文物总览、统一数据结构、卡片、名称搜索、年代/材质/器物类型筛选、通用详情、导航、缺失数据处理、现有贾湖功能保留和异常降级；
- 正式第二件文物仍依赖团队提供经过审核的资料，不能用虚构内容代替；
- v0.3 第一个正式开发任务“统一文物数据结构与贾湖骨笛迁移”已经完成；
- v0.3 第二个正式开发任务“文物总览区域与通用文物卡片”已经完成；
- v0.3 第三个正式开发任务“名称搜索与基础筛选”已经完成；
- v0.3 第四个正式开发任务“通用详情路由与友好错误页面”已经完成；
- v0.3 第五个正式开发任务“统一异常降级与版本收口”已经完成自动检查，人工浏览器验收仍待执行；
- 以上文档、Lint 基线和 v0.3 开发修改均尚未提交 Git。
