# 项目当前状态

## 状态快照

- 检查日期：2026-08-16
- 当前阶段：v0.5 文物快速接入框架收口
- 项目路径：`D:\zzu\competence\音韵河南\henan-music-archaeology`
- 当前分支：`codex/handoff-baseline`
- 远程仓库：`https://github.com/yyyyyyuts/henan-music-archaeology.git`
- Node.js：本地 v24.15.0；CI 使用 Node.js 22
- 内容记录：3 条；贾湖骨笛为明确标注的 Demo，文物 002、003 为公开“资料整理中”占位记录
- 正式新增文物：0 条；本轮未录入或推断新的考古事实

## 已完成框架

- 一件文物一个 TypeScript 数据文件，由统一注册表提供内部、目录可见和正式详情查询。
- `artifact:new` 生成并注册 `draft + internal` 草稿，保护重复编号、slug、非法 slug 和已有目标文件。
- `artifact:check` 检查注册、字段引用、来源定位、审核日期、授权状态和图片/GLB/音频文件，并按文物输出中文报告。
- 正式记录区分内容审核人 `reviewer/reviewedAt` 与素材审核人 `assetReviewer/assetsReviewedAt`。
- 有公开图片、GLB 或文件音频的 `approved/published` 记录必须具备素材审核记录；纯文字正式记录不强制。
- 正式来源需要稳定 ID、名称以及出版物信息或可核对链接；审核日期统一为 `YYYY-MM-DD`。
- 正式公开素材授权只接受 `authorized` 或 `open_license`。
- 3D 查看器支持程序化 Demo、GLB、加载/旋转/缩放/自动旋转/复位和静态备用图降级。
- 音频播放器支持浏览器合成与文件音频、列表切换、播放/暂停、进度、音量及失败降级。
- 占位详情带 `noindex, nofollow`，不渲染专业档案、来源、3D、音频或问答。
- `npm run verify` 统一执行 lint、类型检查、资料预检、生产构建和全部测试；`npm test` 也会先运行资料预检。
- `.github/workflows/quality.yml` 在 Node.js 22 上执行 `npm ci` 和 `npm run verify`。

## 2026-08-16 验收结果

| 检查 | 结果 |
| --- | --- |
| `npm.cmd run verify` | 通过 |
| 文物资料预检 | 0 错误、0 警告 |
| 自动测试 | 22/22 通过 |
| 脚手架保护 | 生成、注册、重复保护、非法输入和目标文件保护均通过 |
| 正式素材规则 | 来源、定位、日期、内容审核、素材审核、授权和文件存在性均有通过/失败用例 |
| 临时正式接入演练 | `artifact-900` 的图片、有效 GLB、双 WAV、切换/播放/暂停/音量控件和损坏文件降级均通过 |
| 临时数据清理 | 临时 worktree、测试公开记录和测试公开素材已移除；正式仓库未保留 `artifact-900` |
| 浏览器 375px | 总览、文物 003 占位详情、贾湖详情无横向溢出，3D/音频存在 |
| 浏览器 440px | 总览、文物 002 占位详情、贾湖详情无横向溢出，3D/音频存在 |
| 浏览器 1440px | 总览、文物 002 占位详情、贾湖详情无横向溢出，3D/音频存在 |
| 浏览器控制台 | 验收路由无 error/warning |
| Word 模板 | 模板 5 页、示例 5 页、交付说明 1 页；逐页视觉检查、表格几何和无障碍审计通过 |

测试素材仅位于 `tests/fixtures/artifact-media/`，并明确标注为非文物、非扫描、非复原的自动测试夹具，不进入正式 `public/`。

## 人工发布步骤

1. 团队使用仓库外层私有 `项目资料/文物资料交付模板/` 交付 Word、素材和授权文件；空缺内容写“待确认”。
2. 运行 `artifact:new` 生成内部草稿，保持 `id`、`slug` 和 `displayIndex` 稳定。
3. 按表录入内容、稳定子记录 ID、来源和字段定位。
4. 内容审核人核对专业文字、争议表述、来源定位和资料版本。
5. 素材审核人核对图片、GLB、音频的授权范围、署名和公开文件。
6. 只把审核与授权完成的素材复制到 `public/artifacts/<slug>/`，运行 `npm run verify`。
7. 人工确认两类审核记录后，再把状态改为 `approved` 或 `published`；没有自动发布命令。

## 当前阻塞与已知问题

- 团队尚未交付文物 002、003 的正式名称、专业资料、来源定位和授权素材，因此继续保持中性占位状态。
- `.openai/hosting.json` 保留原项目 ID `appgprj_6a550ec3cfc48191a94753a7b46cbcc0`；该 Sites 项目当前不可访问，本轮按约定不修改、不恢复、不部署。
- 生产构建成功，但仍提示单个客户端代码块压缩后大于 500 kB；后续可继续拆分非首屏交互代码。
- Vinext 仍提示部分动态路由无法在构建时完全分类；当前生产预览与自动测试均通过。
- 微信 WebView 和目标馆内设备仍需要在真实设备上做兼容性验收。

## 团队仍需提供

- 正式文物清单、稳定编号和名称确认。
- 专业文字、研究争议、时间线、问答及逐项来源定位。
- 内容审核人、审核日期和资料版本。
- 图片、GLB、音频的制作者、来源、授权范围、署名文本，以及素材审核人和审核日期。
- GLB 的单位、比例、朝向、备用图和模型性质说明。
- 后续部署平台、域名、备案和目标设备验收安排。

## 下一步

框架侧暂无必须继续扩展的展示功能。收到团队资料后，优先完整走一遍“生成草稿 → 录入 → 两类审核 → 素材复制 → `verify` → 人工发布”，再根据首批真实资料暴露的问题调整模板或校验规则。
