# 分阶段开发路线图

路线图遵循“一次只做一个可测试的小任务”。阶段编号表示建议顺序，不表示必须在同一时间连续完成。缺少团队资料时，保留现有占位内容和明确标识，不自行补写。

## 阶段 0：建立项目基线（本轮）

- **目标：** 查清实际结构、运行方式、现有功能和风险，建立项目规则与状态文档。
- **输入资料：** 当前仓库、现有 Demo 和用户给出的项目边界。
- **修改范围：** 仅根目录项目文档；不改页面和专业内容。
- **验收标准：** 五份文档齐全；构建、测试、类型检查和 Lint 均有真实记录；问题按优先级列出。
- **风险：** 把静态代码检查误写成浏览器实测，或把待审核内容写成已确认。
- **是否依赖团队资料：** 否。

## 阶段 1：修复类型检查基线

- **目标：** 让完整 TypeScript 检查通过，并增加固定的 `typecheck` 命令。
- **输入资料：** 当前 Cloudflare Worker/Vinext 配置和已安装依赖。
- **修改范围：** TypeScript/Worker 类型配置、`package.json` 脚本；不改页面行为和数据内容。
- **验收标准：** `npm.cmd run typecheck` 退出码为 0；`npm.cmd run build` 和 `npm.cmd test` 继续通过；没有通过关闭严格检查来规避错误。
- **风险：** 错误地引入不匹配的 Worker 类型或顺手升级依赖体系。
- **是否依赖团队资料：** 否。

## 阶段 2：补齐 3D 静态降级

- **目标：** WebGL、观察器或模型初始化失败时显示参考图片和明确提示，页面其他内容仍可使用。
- **输入资料：** 当前参考图和现有演示模型说明。
- **修改范围：** 当前 3D 组件、少量样式和相关测试。
- **验收标准：** 正常环境仍可旋转、缩放和复位；模拟失败时显示静态替代内容；不会把替代图或程序模型说成真实扫描。
- **风险：** 异常捕获范围过大，掩盖真实代码错误；移动端提示遮挡页面。
- **是否依赖团队资料：** 否。

## 阶段 3：逐步拆分可复用组件

- **目标：** 降低 `HeritageDemo.tsx` 的职责，先一次拆一个组件，不改变现有视觉和内容。
- **输入资料：** 当前 3D、声音、问答实现和现有测试快照。
- **修改范围：** 每个子任务只拆分一个模块，例如查看器、声音体验或本地问答。
- **验收标准：** 每次拆分后页面输出和交互不变；构建、类型检查、测试通过；组件接口有清晰类型。
- **风险：** 一次拆分过多导致回归，或把贾湖专用逻辑只换一个“通用”名字却仍然硬编码。
- **是否依赖团队资料：** 否。

## 阶段 4：完善 Artifact 数据结构和多文物页面骨架

- **目标：** 让名称、档案、来源、图片、模型、声音、热点和审核记录都由当前文物数据驱动；新增文物不复制详情页。
- **输入资料：** 团队确认的字段表、文物编号规则和至少第二件文物的审核资料。
- **修改范围：** `Artifact` 类型、数据文件、文物总览、按 slug 读取的统一详情页和来源绑定。
- **验收标准：** 当前文物继续显示；第二件文物仅通过新增数据即可进入总览和同一详情页；当前页面只显示本件文物的来源。
- **风险：** 在没有审核资料时虚构第二件文物；旧字段迁移造成现有 Demo 回归。
- **是否依赖团队资料：** 是。

## 阶段 5：接入真实模型、热点和声音资料

- **目标：** 通用查看器支持有正规来源的 GLB；热点说明和声音完全由数据配置；声音类型清晰标注。
- **输入资料：** 授权 GLB、比例和朝向说明、热点坐标与专业说明、音频文件及分类和授权。
- **修改范围：** 资源数据结构、加载器、热点交互、声音组件、加载状态和失败降级。
- **验收标准：** 模型可加载、旋转、缩放、复位；热点与说明/声音对应；失败时有静态替代；四类声音标识准确。
- **风险：** 模型过大、坐标不一致、移动端性能不足、音频授权或性质标错。
- **是否依赖团队资料：** 是。

## 阶段 6：资料驱动的 AI 讲解员

- **目标：** 通过服务端接口调用现成大模型，只依据团队审核资料作答并提供来源。
- **输入资料：** 审核后的结构化资料、标准问答、参考文献、争议说明、拒答规则和 API 使用预算。
- **修改范围：** 服务端接口、检索/上下文组装、前端讲解窗口、来源显示、错误和离线降级。
- **验收标准：** 密钥不进入浏览器；有资料的问题附来源；无依据时回复“当前资料无法确认”；接口失败时仍可浏览资料和使用固定问答。
- **风险：** 模型编造、引用错配、密钥泄露、费用失控和用户问题日志涉及隐私。
- **是否依赖团队资料：** 是。

## 阶段 7：分层讲解与轻量传播功能

- **目标：** 依次实现三种讲解模式、数字纪念卡，再接入一段已审核的 AIGC 科普视频。
- **输入资料：** 同一套审核资料的三种表达稿、项目视觉规范、纪念卡文案、视频审核稿和素材授权。
- **修改范围：** 每次只实现其中一项；纪念卡使用浏览器 Canvas，视频作为普通文件接入。
- **验收标准：** 三种模式不新增事实；纪念卡不采集真实照片和敏感信息；视频明确标注 AIGC 性质；移动端可用。
- **风险：** 儿童版过度简化导致事实变化，分享图片字体兼容，AIGC 画面被误解为复原结论。
- **是否依赖团队资料：** 是。

## 阶段 8：小程序入口和正式部署验收

- **目标：** 先通过 `web-view` 复用正式网站，并建立国内可访问的稳定部署和离线演示备份。
- **输入资料：** 正式域名、部署平台/账号、备案信息、小程序主体和业务域名配置、隐私与版权文本。
- **修改范围：** 部署适配、环境配置、轻量小程序外壳、兼容性测试和演示视频/截图。
- **验收标准：** 国内常见网络和微信内置浏览器可访问；核心页面、3D、声音和 AI 的成功/失败路径均有记录；密钥和内部素材不公开；存在离线备份。
- **风险：** `chatgpt.site` 国内访问受阻、目标平台不兼容 Worker、小程序业务域名和备案条件不满足。
- **是否依赖团队资料：** 是。

