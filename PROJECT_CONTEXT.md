# 项目背景与范围

## 1. 项目定位

本项目围绕河南代表性音乐考古文物，探索数字展示、文旅传播和文化活化方式。网站是当前最主要的数字成果，整体体验概括为：

> 可看、可听、可玩、可问、可传播。

现阶段优先保证核心页面稳定、资料边界清楚、演示素材标识准确，并逐步形成可以扩展到多件文物的统一结构。

## 2. 当前 Demo

当前版本为“v0.5 多文物展示与交互完善版”，实际入口为根路径 `/`、独立总览 `/artifacts` 和通用详情路径 `/artifacts/[slug]`。当前正式数据仍只有贾湖骨笛。

现有代码已经实现：

- 单件文物展示；
- 统一文物数据结构、独立文物总览、整卡跳转、名称搜索和三类筛选；
- 使用统一 `slug` 的通用详情路由、返回总览和友好404；
- 参考图片、基本资料、交互时间轴、完整事件总览、资料来源和更新时间；
- Three.js 程序生成的 3D 功能演示模型；
- 旋转、缩放、自动旋转和一键复位；
- 浏览器生成的数字合成 WAV 演示音频；
- 基于预设问答和关键词匹配的本地规则问答；
- 浏览器本地生成、预览和下载的数字纪念卡，以及正反面 3D 旋转预览；
- 内容审核状态和资料来源展示；
- “概念验证 Demo”“非真实扫描”“非原器录音”等提示；
- 图片、3D、音频、时间线、来源和问答的模块级异常降级；
- 桌面、平板、手机响应式布局和减少动画样式。

上述功能是概念验证，不代表最终研究成果。自动检查已经通过，本轮页面与纪念卡已检查 390px 至 1440px 布局；375px、真实设备、微信 WebView、真实 WebGL/音频失败路径和专业内容仍需人工与团队继续验收。

## 3. 实际技术栈

- React 19；
- TypeScript 5；
- Next App Router 风格的 `app` 目录；
- Vinext 作为实际开发、构建和启动工具；
- Vite 8；
- Three.js；
- 自定义 CSS；
- Tailwind CSS/PostCSS 已安装，但当前页面主要使用自定义样式；
- Cloudflare Worker 服务入口和 Sites 构建插件；
- TypeScript 数据文件；
- npm 和 `package-lock.json`。

D1、Drizzle 和 R2 相关模板仍在仓库中，但当前产品没有启用正式数据库。

## 4. 我的角色

项目技术工作目前主要由一名软件工程专业本科生负责，并借助 AI 完成代码编写、修改、测试和文档整理。

职责是：

- 技术负责人；
- 功能拆分和任务安排；
- 代码与素材整合；
- 构建、测试和演示准备；
- 与专业成员确认资料边界。

不承担的职责是：

- 自行撰写或判断考古学结论；
- 自行确认文物年代、用途、材质、音色或研究价值；
- 自行把待审核内容改成已审核；
- 自行确认图片、模型、声音和视频的版权授权。

## 5. 专业资料边界

考古学内容必须由团队专业成员提供和审核。技术人员只按审核稿录入，不改变专业含义。

页面内容需要区分：

1. 考古事实；
2. 研究推测或存在争议的解释；
3. 数字演示；
4. 艺术创作。

缺少资料时保留占位内容和明确提示，不使用 AI 自动补写专业事实。

## 6. 后续核心工作

- 接入第二件及后续文物的审核资料与授权资产；
- 使用真实授权 GLB、热点坐标和音频资料验证既有通用框架；
- 只依据审核资料、回答带来源的 AI 讲解；
- 儿童科普、普通参观、研究拓展三种讲解模式；
- 经过审核并明确标注的 AIGC 科普视频；
- 使用 `web-view` 复用网站的微信小程序入口；
- 国内部署、全设备测试和离线演示备份。

详细阶段见 `ROADMAP.md`。

## 7. 暂时不做

- 自训练或自行部署大模型；
- 文物损伤识别和科学修复；
- 复杂知识图谱；
- 游客大数据推荐和用户画像；
- 实时数字人；
- 人脸识别、换脸和用户照片处理；
- 社区、商城和支付；
- 大型 VR/AR 展厅；
- 区块链数字藏品；
- 原生 App。

## 8. 团队需要提供的资料

- 正式项目名称、项目简介、项目标识和版权主体；
- 拟展示文物清单和每件文物的唯一编号；
- 每件文物逐字段审核的名称、年代、材质、尺寸、来源和说明；
- 每条专业陈述对应的文献、页码或可核对位置；
- 审核人、审核状态、审核时间和最后更新时间；
- 图片原文件、作者或机构、授权方式和授权凭证；
- GLB 模型、制作方、授权证明、比例、朝向和热点资料；
- 音频文件、声音类型、录制或制作说明和授权证明；
- AI 讲解使用的审核资料、标准问答、专业词表和拒答规则；
- 三种讲解模式的审核文稿；
- AIGC 视频脚本审核稿和素材授权；
- 正式域名、部署平台、备案安排、小程序主体和业务域名资料。
