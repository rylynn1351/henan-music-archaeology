# 项目背景与范围

## 1. 项目定位

本项目围绕河南代表性音乐考古文物，探索数字展示、文旅传播和文化活化方式。整体体验概括为“可看、可听、可玩、可问、可传播”。

网站是当前最主要的数字成果。现阶段重点不是一次性完成所有设想，而是先保证核心页面稳定、资料边界清楚、演示素材标识准确，并为后续增加文物和交互能力留下可维护的结构。

## 2. 当前版本

当前版本为“贾湖骨笛数字展示 Demo v0.2”，页面入口为根路径 `/`。

已经存在的能力：

- 单件文物展示；
- 文物参考图片、基本资料、时间线和资料来源；
- 程序生成的 Three.js 3D 功能演示模型；
- 浏览器生成的数字合成演示音频；
- 基于已配置问答的本地规则讲解；
- 内容审核状态、资料来源和最后更新时间；
- 375px 手机端和 1440px 电脑端的基础响应式样式；
- “概念验证 Demo”“非真实扫描”“非原器录音”等说明。

当前公开演示地址为：

`https://jiahu-heritage-demo.hannahtrent2954.chatgpt.site`

该地址在国内网络中出现过访问受阻，只能作为现有演示地址记录，不能视为最终部署方案。

## 3. 实际代码结构与数据流

核心页面链路：

```text
app/layout.tsx
  -> app/page.tsx
  -> app/HeritageDemo.tsx
  -> app/ArtifactDetail.tsx
  -> app/heritage-data.ts
```

主要目录和文件：

```text
app/
  page.tsx                 根页面入口
  layout.tsx               根布局、元数据和全局样式入口
  HeritageDemo.tsx         页面布局、3D、合成音频和本地问答
  ArtifactDetail.tsx       文物档案展示组件
  heritage-data.ts         文物、来源、审核状态和问答数据
  globals.css              自定义样式和响应式规则
  chatgpt-auth.ts          当前未使用的站点认证模板代码
public/
  jiahu-bone-flute.jpg     当前页面使用的参考图片
tests/
  rendered-html.test.mjs   服务端渲染和关键内容测试
worker/index.ts            Vinext/Cloudflare Worker 服务入口
build/sites-vite-plugin.ts Sites 构建插件
db/、drizzle/、examples/   当前未启用的数据库及示例模板
```

当前没有第二个正式页面路由，也没有第二件文物。`examples/d1` 下的路由只是模板示例，不属于现有产品页面。

## 4. 实际技术栈

- React 19；
- TypeScript 5；
- Next App Router 风格的 `app` 目录和类型接口；
- Vinext 作为实际开发、构建和启动工具；
- Vite 8；
- Three.js；
- 自定义 CSS；
- Tailwind CSS/PostCSS 已安装和启用，但当前页面主要使用自定义类；
- Cloudflare Worker 运行入口和 Sites 构建插件；
- TypeScript 数据文件；
- npm 和 `package-lock.json`。

D1、Drizzle 和 R2 相关能力尚未在产品中启用。`.openai/hosting.json` 中 `d1` 和 `r2` 均为 `null`，当前没有正式数据库。

## 5. 长期功能方向

- 多文物总览和统一详情页；
- 可复用的 3D 文物查看器及热点；
- 声音实验室和一件重点虚拟乐器；
- 只使用团队审核资料、带来源的 AI 讲解员；
- 儿童科普、普通参观、研究拓展三种讲解模式；
- 数字纪念卡和分享海报；
- 一段经过审核并明确标识的 AIGC 科普视频；
- 通过 `web-view` 复用网站的微信小程序入口；
- 内容审核、资产授权和版本记录；
- 手机、电脑、微信浏览器的测试及异常降级。

以上方向按 `ROADMAP.md` 分阶段推进，不应在一次任务中全部实现。

## 6. 暂不开发

- 自训练或自行部署大模型；
- 文物损伤识别和科学修复；
- 复杂知识图谱和游客大数据推荐；
- 实时数字人、人脸识别或换脸；
- 社区、商城和支付；
- 大型 VR/AR 展厅；
- 区块链数字藏品；
- 原生 App。

## 7. 专业资料边界

开发人员负责技术实现、功能整合、测试和文档，不负责确认考古结论。所有专业内容必须能对应团队提供的来源和审核记录。页面内容分为：

1. 考古事实；
2. 研究推测或存在争议的解释；
3. 数字演示或艺术创作。

三类内容必须明确区分。具体规则见 `CONTENT_AND_ASSET_RULES.md`。

## 8. 团队后续需要提供的资料

- 正式项目名称、简介、项目标识和版权主体；
- 拟展示文物清单及每件文物的唯一编号；
- 每件文物逐字段审核的名称、年代、材质、尺寸、出土信息、收藏来源和说明；
- 每条专业陈述对应的资料来源、页码或可核对位置；
- 审核人、审核结论和审核时间；
- 图片原文件、作者或机构、授权方式、使用范围及授权凭证；
- GLB 模型、模型来源、授权证明、比例/朝向说明和热点资料；
- 音频文件、声音类型、录制或制作方、授权证明及说明；
- 标准问答、专业词表、争议说明和引用来源；
- AIGC 视频脚本的专业审核稿及素材授权；
- 最终域名、部署账号、备案安排和国内网络验收负责人。

