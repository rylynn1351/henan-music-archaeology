# 软件需求规格说明书

## 1. 文档信息

- **项目名称：** 河南音乐考古资源数字活化网站（当前页面名称“豫音焕新声”）
- **当前基线：** v0.3 多文物展示基础版
- **目标版本：** v0.3 多文物展示基础版
- **文档状态：** v0.3 自动验收收口版，人工浏览器验收待完成
- **编制日期：** 2026-07-23

本文件描述 v0.3 要做什么、暂时不做什么，以及如何验收。当前功能和自动检查已完成，375px、1440px及真实浏览器交互仍按第14节人工验收。

## 2. 文档目的

v0.2 已能展示一件贾湖骨笛，并提供图片、文物档案、程序生成的 3D 演示模型、数字合成音频、本地规则问答、来源和审核状态。

v0.3 的目标不是增加大型功能，而是先建立多文物展示的基础结构：

- 从“固定展示一件文物”过渡到“数据结构和页面可以承载多件文物”；
- 提供文物总览、卡片、搜索、筛选和通用详情入口；
- 保留贾湖骨笛现有功能和内容边界；
- 在缺少资料或单项功能失败时，页面仍然可以浏览。

## 3. 项目背景

项目围绕河南代表性音乐考古文物，探索数字展示、文旅传播和文化活化方式，整体体验概括为“可看、可听、可玩、可问、可传播”。

技术负责人主要承担代码实现、功能整合、测试和演示准备。考古学内容、研究判断、审核结论和数字资产授权必须由团队专业成员提供或确认，不能由开发人员或 AI 自行补充。

## 4. 当前基线

截至v0.3自动验收收口时，当前工作区已经存在：

- 根路径 `/`；
- 统一的 `Artifact` TypeScript 类型和 `artifacts` 数组；
- 由统一数据驱动的文物总览、卡片、名称搜索和三类筛选；
- `/artifacts/[slug]` 通用详情路由及友好404；
- `ArtifactDetail` 文物档案区；
- Three.js 程序生成模型；
- 浏览器生成的 WAV 合成演示音频；
- 预设问答和关键词匹配；
- 资料来源、审核状态和更新时间；
- 图片、3D、音频、时间线、来源和问答的模块级降级；
- 720px、980px CSS 响应式断点；
- Lint、TypeScript、构建和12项服务端渲染、数据、路由及降级测试。

当前正式数据仍只有贾湖骨笛；独立 `/artifacts` 总览路由、正式第二件文物和人工浏览器验收不属于已完成项。

## 5. v0.3 项目范围

### 5.1 本次包含

1. 项目首页；
2. 文物总览；
3. 统一文物数据结构；
4. 文物卡片；
5. 名称搜索；
6. 年代筛选；
7. 材质筛选；
8. 器物类型筛选；
9. 通用文物详情页；
10. 总览与详情之间的导航；
11. 不存在的文物地址和缺失字段处理；
12. 保留贾湖骨笛现有图片、3D、音频、本地问答、来源和审核状态；
13. 375px 手机端和 1440px 电脑端适配；
14. 单项功能失败时的降级显示。

### 5.2 范围说明

- “多文物展示基础版”首先指数据、路由和组件能够承载多件文物。
- 如果团队尚未提供第二件文物的审核资料，公开页面可以暂时只有贾湖骨笛一张正式卡片。
- 可以用不进入正式页面的测试夹具验证多记录搜索和筛选，但不得用虚构考古内容充当正式第二件文物。
- v0.3 继续使用 TypeScript 数据文件，不使用数据库。

## 6. 用户角色

### UR-001 普通参观者

浏览项目介绍、文物总览和文物详情，使用搜索、筛选、3D、音频和本地问答。

### UR-002 团队专业成员

核对文物字段、来源、审核状态、研究提示和数字资产标识，不直接通过网站后台编辑数据。

### UR-003 技术维护人员

按审核稿录入数据，维护页面和测试，不能自行修改专业结论或授权状态。

### UR-004 项目评审或演示观众

在电脑或手机上快速理解项目定位、当前成果、资料边界和后续扩展能力。

## 7. 主要使用场景

### UC-001 从首页进入文物总览

用户进入项目首页，了解项目定位，点击入口查看全部已录入文物。

### UC-002 搜索文物

用户在总览页输入文物名称的一部分，页面显示名称匹配的文物卡片。

### UC-003 按标签筛选

用户选择年代、材质或器物类型，页面显示同时满足已选条件的文物。

### UC-004 查看文物详情

用户点击卡片进入对应文物详情，查看档案、来源、审核状态和已配置的数字体验。

### UC-005 返回总览

用户从详情页返回总览，并能继续浏览其他文物。

### UC-006 处理缺失或错误

当文物地址不存在、字段缺失或某个体验模块不可用时，用户看到清楚提示，其他内容仍可使用。

## 8. 功能需求

### FR-001 文物总览

- **功能描述：** 展示所有允许在当前版本公开显示的文物记录。
- **输入：** `artifacts` 数据集合。
- **处理：** 读取可展示记录，生成文物卡片列表；初始状态不附加搜索或筛选条件。
- **输出：** 文物卡片、结果数量和进入详情的入口。
- **异常情况：** 没有可展示记录时显示“当前暂无可展示文物”，不显示空白页或报错堆栈。
- **验收标准：** 当前贾湖骨笛至少显示一张卡片；列表数据来自统一数据文件，不在页面中复制文物字段。

### FR-002 文物搜索

- **功能描述：** 按文物名称进行本地搜索。
- **输入：** 用户输入的搜索文字。
- **处理：** 去除首尾空格，按名称进行包含匹配；空输入恢复全部结果。
- **输出：** 匹配的文物卡片和结果数量。
- **异常情况：** 没有结果时显示明确提示，并提供清空搜索的方式。
- **验收标准：** 输入完整名称或名称中的连续文字均能得到正确结果；搜索不会修改原始数据，也不会发起网络请求。

### FR-003 文物筛选

- **功能描述：** 支持年代、材质和器物类型三个预设维度的本地筛选。
- **输入：** 用户选择的年代、材质、器物类型。
- **处理：** 同一维度第一阶段只选择一个值；不同维度同时选择时使用“同时满足”规则；筛选选项从规范字段生成。
- **输出：** 符合条件的文物卡片、已选条件和重置入口。
- **异常情况：** 没有匹配记录时显示无结果提示；某件文物缺少筛选字段时不得错误归类。
- **验收标准：** 三类筛选可以单独使用和组合使用；重置后恢复全部结果；页面不把规则筛选描述成智能推荐。

### FR-004 通用文物详情

- **功能描述：** 使用同一详情页面结构展示不同文物。
- **输入：** 文物 `slug`。
- **处理：** 根据 `slug` 查找对应 `Artifact`，并按该记录装配档案、时间线、来源、审核状态和可用体验模块。
- **输出：** 对应文物详情页。
- **异常情况：** `slug` 不存在时进入友好错误页；可选模块缺失时显示降级提示，不影响档案区。
- **验收标准：** 页面不固定读取 `artifacts[0]`；详情中的来源只属于当前文物；新增文物不需要复制一份详情页面。

### FR-005 3D 展示

- **功能描述：** 在贾湖骨笛详情中保留当前程序生成的 Three.js 演示模型及其标识。
- **输入：** 文物模型配置和用户的旋转、缩放、复位操作。
- **处理：** 仅当文物配置了受支持的模型类型时加载查看器。
- **输出：** 可交互模型、操作说明和模型性质提示。
- **异常情况：** WebGL、模型初始化或加载失败时显示静态图片和“3D 暂不可用”提示。
- **验收标准：** 贾湖骨笛现有旋转、缩放、自动旋转和复位能力保留；程序模型仍明确标注为非真实扫描、非正式复原。

### FR-006 声音体验

- **功能描述：** 在贾湖骨笛详情中保留浏览器生成的 WAV 合成演示音频。
- **输入：** 文物音频配置和用户播放、暂停、进度、音量操作。
- **处理：** 按当前方式创建音频资源并在不再使用时释放 Object URL。
- **输出：** 原生音频控件、声音标题、说明和声音性质标识。
- **异常情况：** 浏览器不能创建或播放音频时显示“演示音频暂不可用”，其他页面内容继续显示。
- **验收标准：** 当前合成音频可以播放、暂停和再次播放；不得把它描述为原器或复原乐器录音。

### FR-007 本地问答

- **功能描述：** 保留基于当前文物预设问答和关键词的本地规则问答。
- **输入：** 推荐问题按钮或用户输入的文字。
- **处理：** 优先进行标准问题匹配，再进行关键词匹配；无匹配时返回固定的资料不足提示。
- **输出：** 用户问题和本地答案。
- **异常情况：** 问题为空时不发送；当前文物没有问答数据时显示“当前暂无问答资料”。
- **验收标准：** 不调用正式大模型，不上传用户问题，不根据模型自由补充考古事实。

### FR-008 资料来源

- **功能描述：** 展示当前文物对应的资料来源和数字资产说明。
- **输入：** 当前文物的 `sources`。
- **处理：** 只显示当前文物记录的来源。
- **输出：** 来源名称、说明和外部链接。
- **异常情况：** 来源编号无法解析时显示“来源信息待补充”，同时保留其他有效来源。
- **验收标准：** 不再在单件详情中无条件遍历全部全局来源；外部链接使用安全的打开方式。

### FR-009 审核状态

- **功能描述：** 展示每件文物的内容审核状态和最后更新时间。
- **输入：** `reviewStatus`、`reviewer`、`reviewedAt` 和 `updatedAt`。
- **处理：** 按数据原值显示，不由页面推断审核结果。
- **输出：** 审核状态、可读标签和最后更新时间。
- **异常情况：** 审核信息缺失时显示“待专业成员确认”，不得默认为已审核。
- **验收标准：** 贾湖骨笛继续显示当前审核状态；“已审核”必须具备团队确认的审核人和审核时间后才能使用。

### FR-010 异常与降级

- **功能描述：** 防止图片、3D、音频、问答或来源等单项失败造成整页不可用。
- **输入：** 缺失字段、无效地址、运行时异常或不支持的浏览器能力。
- **处理：** 在模块边界捕获或判断异常，使用静态内容、占位图或文字提示替代。
- **输出：** 可继续浏览的详情页和清楚的失败提示。
- **异常情况：** 多个可选模块同时失败时，文物基本档案、来源状态和返回导航仍必须可见。
- **验收标准：** 测试至少覆盖不存在的 slug、缺少图片、缺少 3D、缺少音频和缺少问答五类情况。

### FR-011 项目首页

- **功能描述：** 作为项目门户介绍项目定位，并提供进入文物总览和重点文物的入口。
- **输入：** 项目固定说明、重点文物配置和用户导航操作。
- **处理：** 显示项目简介和概念验证标识，不在首页重复承载所有文物详情。
- **输出：** 首页、总览入口和重点文物入口。
- **异常情况：** 重点文物配置无效时仍保留总览入口。
- **验收标准：** 首页继续明确标注“当前为概念验证 Demo，非最终研究成果”或含义等同的提示。

### FR-012 统一文物数据结构

- **功能描述：** 用同一 `Artifact` 结构管理卡片、筛选、详情、来源、审核和数字资产字段。
- **输入：** 团队审核后的文物字段和授权资产记录。
- **处理：** 对必填字段和可选字段进行类型约束；页面只读取结构化数据。
- **输出：** 可被总览和详情复用的数据集合。
- **异常情况：** 必填字段缺失时阻止记录进入正式公开列表；可选字段缺失时使用明确降级。
- **验收标准：** 新增文物主要通过增加数据完成，不复制详情页面；测试能够识别重复 `id`、重复 `slug` 和无效来源编号。

### FR-013 页面导航

- **功能描述：** 在首页、总览和详情之间提供明确导航。
- **输入：** 用户点击首页入口、卡片、返回总览或面包屑。
- **处理：** 使用站内路由切换到对应页面。
- **输出：** 正确页面和可理解的当前位置。
- **异常情况：** 目标文物不存在时进入友好错误页，并提供返回总览入口。
- **验收标准：** 用户可以完成“首页 → 总览 → 详情 → 总览”的完整路径；键盘可以访问主要链接。

## 9. 非功能需求

### NFR-001 可维护性

- 保留 React、TypeScript、Vinext、Vite、Three.js 和自定义 CSS。
- 不引入数据库、状态管理框架或新的大型依赖。
- 搜索和筛选逻辑优先使用纯函数，便于单元测试。

### NFR-002 性能

- 第一阶段使用本地静态数据和客户端规则筛选。
- 总览卡片图片使用明确尺寸和懒加载。
- 3D 和音频失败不得阻塞基本档案渲染。
- v0.3 不以解决现有大代码块提示为验收条件，但不得无必要扩大首屏依赖。

### NFR-003 可访问性

- 交互元素使用语义化按钮、链接、标签和替代文本。
- 搜索和筛选可通过键盘操作。
- 状态变化和无结果提示应对辅助技术可理解。
- 不只依靠颜色表达审核状态或错误。

### NFR-004 响应式

- 375px 宽度下不出现影响使用的横向滚动、遮挡或控件溢出。
- 1440px 宽度下内容不异常拉伸，卡片和详情保持清楚层级。
- 3D、音频和筛选控件在触屏布局中可以操作。

### NFR-005 可靠性

- `npm run lint`、`npm run typecheck`、`npm run build` 和 `npm test` 必须通过。
- 无效数据和可选功能失败不能造成整页白屏。

### NFR-006 内容可追溯

- 专业陈述必须能关联来源和审核状态。
- 数字演示、研究推测和艺术创作必须与考古事实区分。

## 10. 数据和内容规则

### 10.1 建议的必填字段

- `id`；
- `slug`；
- `name`；
- 卡片所需的年代标签、材质标签、器物类型标签；
- 简要说明；
- 审核状态；
- 最后更新时间；
- 至少一个有效来源编号，或明确的“资料待补充”状态。

### 10.2 建议的可选字段

- 副标题；
- 完整档案字段；
- 时间线；
- 研究提示；
- 3D 模型配置；
- 音频配置；
- 本地问答；
- 扩展图片和热点。

### 10.3 内容约束

- 开发人员不得自行新增、改写或推断考古事实。
- 第二件文物没有审核资料时，不进入正式公开数据。
- 缺少专业字段时显示“资料待补充”或不显示该可选区块，不用 AI 补齐。
- 图片、模型、音频和视频必须记录来源、授权范围和性质。
- 程序模型、数字合成音频和未来 AIGC 内容必须继续显示明确标识。

## 11. 异常与降级要求

| 情况 | v0.3 处理 |
| --- | --- |
| 文物 slug 不存在 | 显示友好错误页和返回总览入口 |
| 总览没有数据 | 显示暂无可展示文物 |
| 搜索或筛选无结果 | 显示无结果和清空条件入口 |
| 主图片缺失或加载失败 | 显示固定比例占位区和文字说明 |
| 3D 不支持或初始化失败 | 显示静态参考图片与不可用提示 |
| 音频创建或播放失败 | 显示不可用提示，保留声音性质说明 |
| 问答数据缺失 | 隐藏输入或显示暂无问答资料 |
| 来源编号无效 | 保留有效来源，并标记缺失来源 |
| 审核状态缺失 | 显示待专业成员确认 |

任何降级都不得把演示素材改写为真实资料。

## 12. 安全和隐私要求

- v0.3 不收集账号、姓名、联系方式、位置或用户画像。
- 搜索、筛选和本地问答不上传用户输入。
- 不保存完整聊天记录。
- 不在前端代码、Git 或文档中写入 API 密钥、令牌和密码。
- 不使用未经审核的 HTML 注入页面。
- 外部来源链接使用 `rel="noreferrer"` 等现有安全属性。
- 当前未启用的 ChatGPT 身份辅助文件不得被误写成已上线用户系统。

## 13. 兼容性要求

- 继续使用项目声明的 Node.js `>=22.13.0` 和 npm 锁文件。
- 在当前 Vinext/Vite 构建方式下通过生产构建。
- 至少人工检查 375px 手机宽度和 1440px 电脑宽度。
- 检查当前主流桌面浏览器；微信内置浏览器作为后续专门验收项。
- 支持 `prefers-reduced-motion` 的现有减弱动画规则。
- 正式公开前仍需补做目标手机、微信 WebView 和国内网络环境验收。

## 14. v0.3 验收标准

### 14.1 自动检查

- [x] Lint 通过；
- [x] TypeScript 类型检查通过；
- [x] 生产构建通过；
- [x] 原有贾湖流程服务端渲染测试继续通过；
- [x] 搜索、筛选、动态详情、展示权限和错误路径测试通过；
- [ ] 集中数据校验函数仍未实现；当前测试已检查正式数据唯一 `id`、唯一 `slug` 和仅一件正式记录。

### 14.2 功能验收

- [x] 首页可以进入文物总览；
- [x] 总览使用统一数据生成卡片；
- [x] 名称搜索、三类筛选及重置行为正确；
- [x] 卡片可以进入对应详情；
- [x] 不存在的 slug 有友好提示；
- [x] 贾湖骨笛现有图片、3D、音频、本地问答、来源和审核状态保留；
- [x] 当前文物详情只读取当前记录的来源；
- [x] 缺失可选字段和单项模块错误具有降级状态；
- [x] 测试夹具没有进入正式文物数据。

### 14.3 人工验收

- [ ] 375px 下没有影响使用的溢出或遮挡；
- [ ] 1440px 下布局层级正常；
- [ ] 图片没有明显变形或异常裁切；
- [ ] 3D 旋转、缩放和复位可用，失败路径有降级；
- [ ] 音频可以播放、暂停和再次播放；
- [ ] 问答、来源、审核状态和 Demo 标识仍然清楚；
- [ ] 浏览器控制台没有阻塞功能的错误。

## 15. v0.3 暂不实现

- 正式大模型接口；
- 三种 AI 讲解模式；
- 真实 GLB 模型接入；
- 第二件文物未经审核的正式资料；
- 数字纪念卡；
- AIGC 视频；
- 微信小程序；
- 用户系统；
- 数据库；
- 知识图谱；
- AR 或 VR；
- 国内正式部署；
- 社区、商城、支付和用户画像。

## 16. 仍依赖团队提供的资料

- 正式文物清单和公开顺序；
- 每件文物稳定且唯一的 `id`、`slug` 命名规则；
- 年代、材质、器物类型三个筛选维度的统一口径；
- 第二件及后续文物逐字段审核资料；
- 每件文物的卡片缩略图、原图、替代文本、署名和授权证明；
- 每条专业内容的来源编号和可核对位置；
- 审核人、审核状态、审核时间和最后更新时间；
- 模型、音频等可选资产的来源、性质、授权和替代图片；
- 项目首页的最终名称、简介、标识和版权主体；
- 是否允许仅凭一件正式文物发布 v0.3 技术基础版。

## 17. 需求变更原则

- v0.3 范围变更需要先更新本文件，再开始编码。
- 新增一项功能时，应说明输入、输出、异常和验收标准。
- 新增专业资料时，必须先完成来源和审核记录。
- 不因演示需要把远期功能临时塞入 v0.3。
