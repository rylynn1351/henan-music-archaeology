# 测试素材说明

本目录只保存自动测试与临时接入演练使用的技术夹具，不是文物资料，也不得复制到正式网站 `public/`。

- `test-only-triangle.glb`：最小三角形 glTF Binary，用于验证 GLB 加载分支。
- `test-only-tone.wav`：短时数字正弦波，用于验证文件音频分支。
- `test-only-pixel.png`：1×1 PNG，用于验证图片文件与备用图引用。

这些文件均由 `generate-fixtures.mjs` 确定性生成，不表达任何考古、复原、音色或扫描结论。
